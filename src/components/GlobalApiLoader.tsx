/**
 * Loader global por requisição (API).
 *
 * IMPORTANTE:
 * O usuário pediu para NÃO mostrar loading durante interações dos jogos
 * (arrastar, virar cartas, etc). Para evitar qualquer overlay inesperado,
 * este componente fica desativado e retorna null.
 *
 * Mantemos o arquivo para não quebrar imports existentes.
 */
export default function GlobalApiLoader(_props: any) {
  return null;
}


